<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                             android:layout_width="match_parent"
                                             android:layout_height="wrap_content"
                                             xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/cloud_top"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:contentDescription="@string/app_name"
        app:layout_constraintTop_toTopOf="parent"
        android:background="@drawable/ic_cloud_top"
        android:scaleType="fitXY"
        />

    <TextView
        android:id="@+id/cloud_nickname"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/cloud_cloud_content"
        android:layout_marginTop="2dp"
        android:textColor="@color/white_bg"
        android:text="NOSAE"
        />

    <TextView
        android:id="@+id/cloud_cloud_content"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/cloud_nickname"
        app:layout_constraintEnd_toEndOf="@id/cloud_nickname"
        app:layout_constraintBottom_toBottomOf="@id/cloud_top"
        android:layout_marginBottom="30dp"
        android:textColor="@color/white_bg"
        android:text="@string/app_name"
        />

</android.support.constraint.ConstraintLayout>